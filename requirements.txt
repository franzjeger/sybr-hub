# Web UI
fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
# Multipart form parsing — FastAPI raises at import time on any route that
# declares File()/Form(), which the backup restore and branding upload do.
python-multipart>=0.0.7,<1.0
# Guacamole reverse proxy (remote access) speaks WebSocket to guacd.
websockets>=12.0,<15.0

# Terminal UI (app/ui) — a separate entry point from the web service, but
# kept declared so the tree imports cleanly and the suite can cover it.
textual>=0.60.0,<2.0


# Microsoft Graph API (httpx-based, no heavy SDK)
httpx>=0.27.0,<1.0
msal>=1.28.0,<2.0

# Azure SDK
azure-identity>=1.16.0,<2.0
azure-mgmt-compute>=30.3.0,<32.0
azure-mgmt-network>=25.3.0,<27.0
azure-mgmt-resource>=23.1.0,<24.0
azure-mgmt-storage>=21.1.0,<22.0
azure-mgmt-monitor>=6.0.1,<7.0
azure-mgmt-advisor>=9.0.0,<10.0
azure-mgmt-recoveryservices>=2.5.0,<3.0
# Separate distribution from azure-mgmt-recoveryservices — the Azure
# governance section imports it for backup-policy checks, and without it
# that check silently degrades to "unavailable".
azure-mgmt-recoveryservicesbackup>=8.0.0,<10.0
azure-mgmt-loganalytics>=13.0.0,<14.0
azure-mgmt-desktopvirtualization>=1.0.0,<2.0
# Cost analysis, RBAC role assignments and tenant/subscription enumeration
# for the Azure governance sections.
azure-mgmt-costmanagement>=4.0.0,<6.0
azure-mgmt-authorization>=4.0.0,<5.0
azure-mgmt-resource-subscriptions>=1.0.0b1,<2.0

# Credentials
keyring>=24.3.0,<26.0

# Cryptography (cert generation, JWT signing via PyJWT[crypto])
# 50.0.0 clears the 2026 advisories reported by pip-audit. Keep this aligned
# with pyOpenSSL, whose 26.4 line is the first to support cryptography 49+.
cryptography>=50.0.0,<51.0

# Reports
jinja2>=3.1.4,<4.0
weasyprint>=69.0,<70.0

# Sanitizes third-party HTML before it can be rendered by the same-origin web
# proxy. Regex URL rewriting is not an HTML security boundary.
nh3>=0.3.6,<0.4

# SSH (UniFi direct device access, FortiGate CLI)
asyncssh>=2.14.0,<3.0

# Database
aiosqlite>=0.20.0,<1.0

# Authentication — PyJWT is actively maintained and broadly used (Django,
# DRF, Authlib upstream). python-jose was dropped; it hasn't had a
# meaningful release since 2023 and is deprecated in many downstream projects.
argon2-cffi>=25.1.0,<26.0
PyJWT[crypto]>=2.9.0,<3.0

# DNS validation
dnspython>=2.6.0,<3.0

# Penetration testing module.
# No python-nmap here on purpose: the pentest code shells out to the nmap
# *binary* via subprocess (app/modules/pentest/{scanner,smb_enum}.py check
# shutil.which("nmap") and degrade to "unavailable" when it is absent), and
# never imports the python-nmap library. It was a hard dependency for a module
# nothing imported, and its legacy setup.py fails to build under newer
# setuptools — an install-time crash for a package with no callers.
# TLS auditing needs the low-level handshake control pyOpenSSL exposes and
# the stdlib ssl module does not.
pyOpenSSL>=26.4.0,<27.0

# Uniweb hosting portal has no API — the client drives a headless Chrome
# over the DevTools protocol.
pychrome>=0.2.4,<1.0

# Claude assistant
anthropic>=0.40.0,<1.0

# Utilities
platformdirs>=4.2.0,<5.0
pydantic>=2.7.0,<3.0
six>=1.16.0,<2.0

# Testing
pytest>=9.1.1,<10.0
pytest-asyncio>=1.4.0,<2.0
